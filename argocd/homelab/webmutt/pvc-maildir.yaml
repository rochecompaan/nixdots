apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webmutt-maildir
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: longhorn-sata
