{
  # Niri-specific workspaces module (Waybar: niri/workspaces)
  "niri/workspaces" = {
    # Show the per-output index so ordering is 1..10.
    format = "{index}";
    # all-outputs = false; # default
  };
}
